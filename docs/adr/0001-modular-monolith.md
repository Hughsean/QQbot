# ADR-0001：MVP 采用模块化单体

- 状态：Accepted
- 日期：2026-08-12

## 背景

项目同时涉及 QQ、邮箱、AI、RAG、内部排程和提醒，但只服务单个用户，没有证据需要微服务。过早拆分会增加部署、事务、观测和调试成本。

## 决策

采用模块化单体：同一仓库和部署制品，严格区分模块命名空间、数据所有权和公开契约。异步任务通过 Outbox/Job 边界执行。

## 结果

- 获得简单部署和本地事务。
- 仍需架构测试阻止跨模块导入和跨表访问。
- 未来只有在独立扩缩容、故障隔离或团队所有权出现真实需要时拆分服务。
