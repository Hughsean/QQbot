# ADR-0003：外部内容永远是数据，不是命令

- 状态：Accepted
- 日期：2026-08-12

## 背景

邮件、转发聊天、附件和网页可能包含提示注入或冒充用户指令的文字。依赖模型自行判断其可信度不可靠。

## 决策

只有经过入口身份验证的用户 Direct Command 属于 T1。邮件、转发内容和附件固定为 T2/T3，不能提升信任级别，也不能直接触发外部副作用。

## 结果

- 来源和进入方式必须作为 InboxItem 的强类型字段保存。
- LLM 只能抽取事实和候选结构。
- 用户确认必须来自独立的已认证交互，而不是被引用内容。
